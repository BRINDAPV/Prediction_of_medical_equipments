import React from 'react';
import { Box, Typography } from '@mui/material';

const Maintenance = () => (
  <Box p={3}>
    <Typography variant="h4" gutterBottom>Maintenance & Preventive Actions</Typography>
    <Typography>Shows recommended maintenance schedules and preventive actions for equipment.</Typography>
  </Box>
);

export default Maintenance;