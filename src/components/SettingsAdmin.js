import React from 'react';
import { Box, Typography } from '@mui/material';

const SettingsAdmin = () => (
  <Box p={3}>
    <Typography variant="h4" gutterBottom>Settings / Admin</Typography>
    <Typography>Manage application settings and user administration.</Typography>
  </Box>
);

export default SettingsAdmin;