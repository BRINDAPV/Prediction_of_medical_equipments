import React from 'react';
import { Box, Typography } from '@mui/material';

const RegulatoryAlerts = () => (
  <Box p={3}>
    <Typography variant="h4" gutterBottom>Regulatory Compliance & Alerts</Typography>
    <Typography>Displays alerts related to regulatory compliance of medical equipment.</Typography>
  </Box>
);

export default RegulatoryAlerts;